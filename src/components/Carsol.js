
function Carsol(){
    console.log('b')
        // const categories = plantList.reduce(
        //     (acc, plant) =>
        //         acc.includes(plant.category) ? acc : acc.concat(plant.category),
        //     []
        // )
    
        // return (
        //     <div>
        //         <ul>
        //             {categories.map((cat) => (
        //                 <li key={cat}>{cat}</li>
        //             ))}
        //         </ul>
        //         <ul>
        //             {plantList.map((plant) => (
        //                 <li key={plant.id}>{plant.name} 
        //                     {plant.isBestSale && <span>🔥</span>}


                       // {/* on peut utilise comme ça  {plant.isBestSale ? <span>🔥</span> : null }
                       // ou {plant.isBestSale && plant.category === "classique" && <span>🔥</span>}
                       // ou methode classic {(plant.isBestSale || plant.category === "classique") && <span>🔥</span>} */}
        //                 </li>
        //             ))}
        //         </ul>
        //     </div>
        // )
    
}
export default Carsol